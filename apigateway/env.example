# ============================================
# Configuración del API Gateway
# ============================================

# URL del orquestador (IP privada si están en la misma VPC, IP pública si no)
# ⚠️ CAMBIAR: IP de la instancia donde está el orquestador
ORQUESTADOR_URL=http://172.31.XX.XX:8080

# Nombres de servicios (deben coincidir con los nombres usados al registrar)
GESTOR_PEDIDOS_SERVICE_NAME=gestor-pedidos
MONITOR_SERVICE_NAME=monitor

# URLs de fallback si el service discovery falla
# ⚠️ CAMBIAR: IPs de las instancias donde están los microservicios
GESTOR_PEDIDOS_FALLBACK_URL=http://172.31.XX.XX:5000
MONITOR_FALLBACK_URL=http://172.31.XX.XX:5001

# ============================================
# Ejemplo con IPs reales:
# ============================================
# ORQUESTADOR_URL=http://172.31.78.108:8080
# GESTOR_PEDIDOS_SERVICE_NAME=gestor-pedidos
# MONITOR_SERVICE_NAME=monitor
# GESTOR_PEDIDOS_FALLBACK_URL=http://172.31.74.102:5000
# MONITOR_FALLBACK_URL=http://172.31.XX.XX:5001

